<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorGreen</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorGreen</item>
        <item name="android:statusBarColor">@color/colorGreen</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="EntradaTheme" parent="AppTheme"/>

    <style name="CustomTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/colorGreen</item>
        <item name="android:colorForeground">@color/colorTextToolbar</item>
        <item name="android:textColorPrimary">@color/colorTextToolbar</item>
        <item name="android:windowBackground">@color/colorCustomBack</item>
    </style>

</resources>
